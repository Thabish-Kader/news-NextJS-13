import React from "react";

type Props = {};

const ReadMoreButton = (props: Props) => {
	return (
		<button className="font-semibold bg-red-500 rounded-full p-3 mt-2">
			Read More
		</button>
	);
};

export default ReadMoreButton;
